# SwiftPM build output
.build/
*.xcodeproj

# Built menu-bar app bundle + staged hook/MCP binaries (regenerated by scripts/build-app.sh)
build/

# Machine-local state, not shared across machines
.claude/squirrel-state.json
.claude/settings.local.json
# (The nest under .claude/nest/ is ignored by its own .claude/nest/.gitignore,
#  which Squirrel seeds on first run — a personal parking lot, not a backlog.)

# macOS cruft
.DS_Store
