# Squirrel nest

Auto-managed by squirrel-mcp. Parked ideas for this project — title, a one-line
summary, and the `(slug)` of the file under `.claude/nest/` that holds the full
detail. This index is loaded via CLAUDE.md; open a slug's file only when you act on it.

## (no priority)
- **Fix voice permissions for Squirrel app** — Investigate why Squirrel doesn't appear in System Settings under microphone/accessibility permissions. `(fix-voice-permissions-for)`
- **Link design decisions in docs back to source…** — Add references in CLAUDE.md or INDEX.md pointing to squirrel files that explain specific design decisions. `(link-design-decisions-in)`
- **Move forest.md into a hidden .forest directory** — Relocate forest.md to ~/.forest/ instead of root home directory. `(move-forest-md-into)`
- **Review UI from high level for cuts and additions** — Audit current UI at a high level to identify what to keep, cut, or add. `(review-ui-from-high)`
- **SessionStart hook nudge for unassigned squirrels count** — Add SessionStart hook (not UserPromptSubmit) to surface one-line unassigned-squirrel count at session open. `(sessionstart-hook-nudge-for)`
